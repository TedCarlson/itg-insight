import BpLeadOverviewPageShell from "@/features/role-bp-lead/pages/BpLeadOverviewPageShell";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function Page() {
  return <BpLeadOverviewPageShell />;
}
